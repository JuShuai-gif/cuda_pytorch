# 27 学习清单

## 1. 本文目标

- 给出一份"读完即会用、会改、会说"的核验清单。
- 每个条目标注对应文档与验证方式。

## 2. 知识层（理解）

- [ ] 能讲清 TileLang 是什么、解决什么问题（`01`）
- [ ] 能画出仓库三层架构（Python DSL / C++ IR+pass / CUDA runtime）（`02`）
- [ ] 能说清 TIRX、tvm_ffi、libtilelang.so 的关系（`05, 06`）
- [ ] 能默写 JIT 编译链与 Pass 流水线（`08, 21`）
- [ ] 能区分 tl.transform 与 tirx.transform（`08`）
- [ ] 理解 `@tilelang.jit` 的编译时机与缓存键（`09, 10`）
- [ ] 理解 autotune 的并行编译/测速/缓存（`11`）
- [ ] 理解 CMake 如何引入 TVM、如何重编译（`12, 13`）

## 3. 实践层（会用）

- [ ] 本机成功源码编译并跑通 quickstart（`12`）
- [ ] 能写出并运行：向量加、elementwise、matmul（`14`）
- [ ] 能跑通至少一个 FlashAttention 示例（`14`）
- [ ] 能用 `get_kernel_source()` 查看并解释生成 CUDA（`22`）
- [ ] 能用 `do_bench` 测速并与 cuBLAS 对比（`17`）
- [ ] 能用 `@autotune` 搜索 matmul 最优参数（`11`）
- [ ] 会用 `TL_ENABLE_DUMP_IR` 调试编译（`08, 16`）
- [ ] 能跑 `pytest examples/gemm/test_example_gemm.py`（`15`）
- [ ] 能完成一个 Python 层改动并即时生效（`24`）

## 4. 进阶层（会改/会说）

- [ ] 能新增一个 C++ 打印 pass 并重编译验证（`24`）
- [ ] 能定位 6 类常见问题的根因（`23`）
- [ ] 能解释 TileLang 与 Triton 的取舍（`18`）
- [ ] 能理解 layout 推断在 gemm 中的作用（`19, 20`）
- [ ] 能读懂 `src/op/gemm.cc` 的算子注册（`06, 24`）
- [ ] 能看懂 `tilelang/cuda/pipeline.py` 的 pass 序列（`08, 21`）

## 5. 交付层（面向面试/简历）

- [ ] 完成 `experiments/` 下至少 3 个实验并记录结果
- [ ] 更新 `项目分析状态.md` 的编译状态
- [ ] 能用 10 分钟讲清一个自己实现的 kernel（`29` 模拟）
- [ ] 输出一份"TileLang 学习总结"（`30`）

## 6. 自测题（综合）

1. 从 `torch.randn` 传入到 `mma.sync` 执行，中间发生什么？（`21`）
2. 为什么 TileLang 的 GEMM 能接近 cuBLAS？（`17, 18`）
3. 如果让你给 TileLang 加一个 `T.softmax` 原语，你会怎么做？（`24`）

## 7. 掌握检查

- [ ] 清单勾选 ≥80%
- [ ] 能独立完成一次"设计 kernel → 编译 → 校验 → 调优"全流程

## 8. 下一步

进入 `28_30天学习计划.md`。
