# 27 学习清单

## 1. 本文目标

- 给出一份"读完即会用、会改、会说"的核验清单。
- 每个条目标注对应文档与验证方式。

## 2. 知识层（理解）

- [ ] 能讲清 Triton 是什么、解决什么问题（`01`）
- [ ] 能画出仓库三层架构（Python DSL / MLIR 核心 / 后端插件）（`02`）
- [ ] 能说清 ttir/ttgir/llir、MLIR、triton._C 的关系（`04, 05, 06`）
- [ ] 能默写 JIT 编译链与 pass 流水线（`07, 08, 21`）
- [ ] 理解 `@triton.jit` 的编译时机与缓存键（`09, 10`）
- [ ] 理解 autotune 的 Config/搜索/缓存（`11`）
- [ ] 理解 CMake + nanobind 构建（`12, 13`）

## 3. 实践层（会用）

- [ ] 本机成功源码编译并跑通最小 kernel（`12`）
- [ ] 能写出并运行：向量加、softmax、GEMM（`14`）
- [ ] 能跑通至少一个 FlashAttention 示例（`14`）
- [ ] 能用 `TRITON_DUMP_IR` 导出并解释各阶段 IR（`16, 22`）
- [ ] 能用 `do_bench` 测速并与 cuBLAS 对比（`17`）
- [ ] 能用 `@autotune` 搜索 GEMM 最优参数（`11`）
- [ ] 会用 `triton-opt`/`triton-reduce` 调试（`16`）
- [ ] 能跑 lit 与 pytest 测试（`15`）
- [ ] 能完成一个 Python 层改动并即时生效（`24`）

## 4. 进阶层（会改/会说）

- [ ] 能新增一个 C++ pass 并重编译验证（`24`）
- [ ] 能定位 6 类常见问题的根因（`23`）
- [ ] 能解释 Triton 与 TileLang 的取舍（`18`）
- [ ] 能理解布局系统在 mma 中的作用（`08, 19`）
- [ ] 能读懂 `make_ttgir` 的 pass 流水线（`08, 21`）
- [ ] 能看懂 `third_party/nvidia/backend/compiler.py`（`07, 21`）

## 5. 交付层（面向面试/简历）

- [ ] 完成 `experiments/` 下至少 3 个实验并记录结果
- [ ] 用 10 分钟讲清一个自己实现的 kernel（`29` 模拟）
- [ ] 输出一份"Triton 学习总结"（`30`）

## 6. 自测题（综合）

1. 从 `torch.randn` 传入到 `mma.sync` 执行，中间发生什么？（`21`）
2. 为什么 Triton 的 GEMM 能接近 cuBLAS？（`08, 17`）
3. 如果让你给 Triton 加一个 `tl.softmax` 原语，你会怎么做？（`24`）

## 7. 掌握检查

- [ ] 清单勾选 ≥80%
- [ ] 能独立完成一次"设计 kernel → 编译 → 校验 → 调优"全流程

## 8. 下一步

进入 `28_30天学习计划.md`。
