# 21 核心调用链（深度版）

> 本文目标：把 3 条调用链细化到函数/行号，作为源码导航精确索引。

## 1. 调用链 1：JIT 编译链

```mermaid
flowchart TD
    A["kernel[grid](args)"] --> B["JITFunction.run (jit.py:727)"]
    B --> C["compute_cache_key (jit.py:605)"]
    C --> D["kernel_cache.get (进程内)"]
    D -->|miss| E["_do_compile (jit.py:879)"]
    E --> F["ASTSource (compiler.py:52)"]
    F --> G["compile() (compiler.py:226)"]
    G --> H["get_cache_key (cache.py:319)"]
    H --> I["get_cache_manager (cache.py:258)"]
    I -->|磁盘 miss| J["编译流水线 (make_*)"]
    J --> K["CompiledKernel (compiler.py:407)"]
    I -->|hit| K
```

**关键位置**：
- run：jit.py:727
- _do_compile：jit.py:879
- compile：compiler.py:226
- ASTSource：compiler.py:52
- get_cache_key：cache.py:319
- CompiledKernel：compiler.py:407

## 2. 调用链 2：编译流水线链

```mermaid
flowchart TD
    A["compile() (compiler.py:226)"] --> B["make_backend(target)"]
    B --> C["backend.add_stages (nvidia compiler.py:598)"]
    C --> D["src.make_ir (AST→ttir)"]
    D --> E["make_ttgir (nvidia compiler.py:262)"]
    E --> F["make_llir (nvidia compiler.py:368)"]
    F --> G["make_ptx (nvidia compiler.py:480)"]
    G --> H["make_cubin (nvidia compiler.py:513)"]
    H --> I["CompiledKernel"]
```

`add_stages`（:598）注册：ttir/ttgir/llir/ptx/cubin 五阶段。

## 3. 调用链 3：launch 链

```mermaid
flowchart TD
    A["run() 尾部 (jit.py:780+)"] --> B["计算 grid"]
    B --> C["kernel.launch_metadata"]
    C --> D["kernel.run (launcher)"]
    D --> E["driver.c launchKernel (driver.c:1431)"]
    E --> F["extractArgs → extraction_map"]
    F --> G["_launch (driver.c:982) cuLaunchKernelEx"]
```

## 4. 调用链 4：tl.dot → mma 降级链（深度）

```mermaid
flowchart TD
    A["tl.dot (language)"] --> B["tt.dot (TTGIR)"]
    B --> C["AccelerateMatmul getMMAVersionSafe (AccelerateMatmul.cpp:43)"]
    C --> D["BlockedToMMA (AccelerateMatmul.cpp:490)"]
    D -->|v2| E["#ttg.dot_op + tt.dot"]
    D -->|v3| F["WarpGroupDotOp"]
    D -->|v5| G["TCGen5MMAOp"]
    E --> H["DotOpToLLVM 分派 (nvidia DotOpToLLVM.cpp:74)"]
    H --> I["MMAv2.cpp convertMMA"]
    I --> J["mma.sync PTX"]
```

## 5. 后端发现链

```mermaid
flowchart LR
    A["compile(target)"] --> B["driver.active.get_current_target"]
    B --> C["make_backend (compiler.py:366)"]
    C --> D["_discover_backends (backends/__init__.py:52)"]
    D --> E["nvidia backend"]
```

## 6. 关键源码位置速查

| 想找 | 位置 |
| --- | --- |
| JIT 装饰器 | `jit.py:629` |
| run | `jit.py:727` |
| compile | `compiler.py:226` |
| AST→ttir | `code_generator.py:1712` |
| add_stages | `nvidia/compiler.py:598` |
| make_ttgir | `nvidia/compiler.py:262` |
| make_cubin | `nvidia/compiler.py:513` |
| get_cache_key | `cache.py:319` |
| getMMAVersionSafe | `AccelerateMatmul.cpp:43` |
| MMAv2 降级 | `nvidia/DotOpToLLVM/MMAv2.cpp` |
| CUDA driver | `driver.c:982` |

## 7. 深入自测

1. 4 条链各是什么？
2. `add_stages` 注册哪五阶段？
3. tl.dot 的降级分派逻辑？
4. get_cache_key 在哪？

## 8. 下一步

进入 `22_生成代码与中间产物.md`（深度版）。
