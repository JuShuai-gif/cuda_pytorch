# Triton 项目分析状态

> 本文件记录 Triton 学习文档的编写状态与源码版本信息。随着学习推进持续更新。

## 仓库版本

- 项目名称：Triton（OpenAI 开源 GPU 编程语言）
- 源码：`/home/hpc/ghr_code/triton`
- 版本：3.8.0（`python/triton/__init__.py:2`）
- 当前 commit：`be81991971 [BACKEND] Build LLVM at b010a18d (#11160)`
- 分支：main

## 技术栈

- 语言：Python（`python/triton/`）+ C++/MLIR（`lib/`、`include/`）
- IR：ttir → ttgir → llir → LLVM IR → PTX → cubin
- 布局系统：LinearLayout（GF(2) 矩阵，`lib/Tools/LinearLayout.cpp`）
- 构建：setup.py + CMake + ninja + nanobind（`triton._C`）
- 后端插件：`third_party/nvidia/`（CUDA）、`third_party/amd/`（ROCm）、`third_party/proton/`（profiler）
- 依赖：LLVM/MLIR（构建时拉取）、CUDA Toolkit、ptxas

## 环境信息

| 项目 | 值 |
| --- | --- |
| 操作系统 | Linux（gcc 13.3.0） |
| Python | 3.12（建议 conda 环境 `triton`） |
| CUDA | 12.8（/usr/local/cuda-12.8） |
| GPU | RTX 4090（SM89） |
| CMake | 4.1.3 |
| Ninja | 未安装（需 `pip install ninja`） |

## 文档编写状态

- 全部 41 篇文档（00-40）已编写完成，存放于本目录。
- **深度版**（2026-08-04）：基于源码深挖（LinearLayout/布局推断、mma 降级、pipeliner+JIT/缓存、codegen+backend 四大机制），01-29 篇均已升级为深度版，含：算法逐行解读、IR 变换示例、源码调用栈图、CUDA/硬件对应。
- 关键源码位置标注基于 commit `be81991971`，定位时优先用"文件 + 类名 + 函数名"。

## 尚未验证（运行类）

- [ ] 源码编译成功（conda 环境 + ninja + LLVM）。
- [ ] 最小 kernel 跑通（vector add）。
- [ ] `TRITON_DUMP_IR=1` 产物内容。
- [ ] 与 cuBLAS/TileLang 性能对比。
- [ ] autotune 实际运行。

> 编译步骤见 `12_编译与安装指南.md`；完成实验后回填本节。

## 推荐下一步

1. 按 `12_编译与安装指南.md` 编译（conda 环境、`CMAKE_BUILD_PARALLEL_LEVEL=4`）。
2. 跑 `experiments/10_jit/test_cache.py`、`experiments/11_autotune/tune_gemm.py`、`experiments/16_debug/dump_ir.py`、`experiments/17_perf/bench_gemm.py`。
3. 回填"尚未验证"条目。
