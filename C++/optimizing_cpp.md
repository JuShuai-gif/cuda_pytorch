你现在是一名精通 C++、编译器优化、CPU 微架构、Linux 性能分析和 SIMD 编程的高级工程师。

当前项目中有一个 PDF：

/home/hpc/ghr_code/cuda_pytorch/C++/optimizing_cpp.pdf

这本 PDF 是《Optimizing software in C++》，请你完整分析这本书，并将它整理成一套可以系统学习 C++ 性能优化的中文教程。

你应该在已创建好的目录下：/home/hpc/ghr_code/cuda_pytorch/C++/Optimizing_software_in_C++ 

创建以下目录结构：

├── note/
│   ├── README.md
│   ├── 00_全书导读.md
│   ├── 01_性能优化基本原则.md
│   ├── 02_平台与编译器选择.md
│   ├── 03_性能热点分析.md
│   ├── 04_C++语言结构性能分析.md
│   ├── 05_编译器优化原理.md
│   ├── 06_内存与缓存优化.md
│   ├── 07_多线程优化.md
│   ├── 08_乱序执行与指令级并行.md
│   ├── 09_SIMD与自动向量化.md
│   ├── 10_Intrinsics编程.md
│   ├── 11_CPU指令集分发.md
│   ├── 12_专项优化技巧.md
│   ├── 13_模板与编译期优化.md
│   ├── 14_性能测试与Benchmark.md
│   ├── 15_嵌入式系统优化.md
│   ├── 16_编译参数速查表.md
│   ├── 17_性能优化检查清单.md
│   └── 18_学习路线与实践项目.md
│
├── src/
│   ├── CMakeLists.txt
│   ├── README.md
│   ├── common/
│   │   ├── benchmark.h
│   │   ├── benchmark.cpp
│   │   ├── cpu_info.h
│   │   └── cpu_info.cpp
│   ├── 01_profiling/
│   ├── 02_integer_float/
│   ├── 03_branch/
│   ├── 04_loop/
│   ├── 05_function/
│   ├── 06_class_virtual/
│   ├── 07_container/
│   ├── 08_memory_cache/
│   ├── 09_alignment/
│   ├── 10_multithreading/
│   ├── 11_false_sharing/
│   ├── 12_auto_vectorization/
│   ├── 13_intrinsics/
│   ├── 14_cpu_dispatch/
│   ├── 15_lookup_table/
│   ├── 16_division_optimization/
│   ├── 17_template_metaprogramming/
│   └── 18_benchmark/
│
└── scripts/
    ├── build.sh
    ├── run_all.sh
    ├── benchmark_all.sh
    └── perf_profile.sh

==================================================
一、总体目标
==================================================

不要简单翻译 PDF。

你需要把 PDF 转换成一套：

1. 中文技术笔记；
2. 可编译运行的 C++ 示例；
3. 可重复执行的性能 Benchmark；
4. 可以帮助读者理解 CPU、编译器和代码性能关系的学习材料。

最终读者已经掌握基础 C++，但对以下内容不熟悉：

- CPU Cache；
- 分支预测；
- 流水线；
- 乱序执行；
- 指令延迟和吞吐；
- 自动向量化；
- SIMD；
- AVX、AVX2、AVX-512；
- 编译器优化；
- perf；
- Benchmark 设计。

因此，所有笔记都必须从概念、原理、代码和实验四个角度解释。

==================================================
二、PDF 分析要求
==================================================

先完整读取 PDF 的目录和章节结构，再开始创建文件。

第一步只完成以下工作：

1. 读取 PDF 目录；
2. 建立“PDF 原始章节 → note 文件”的映射；
3. 建立“知识点 → src 实验代码”的映射；
4. 把规划写入：

note/00_全书导读.md

不要在还没有理解全书结构之前随意生成大量文件。

分析时必须遵循以下规则：

1. 所有结论优先来源于 PDF；
2. 不得编造 PDF 中没有出现的观点；
3. 可以补充现代 C++、Linux、GCC、Clang 和 CPU 微架构知识；
4. 补充内容必须明确标记为：

> 补充说明：以下内容不是 PDF 原文，而是为了帮助理解增加的现代背景知识。

5. 每个重要观点都标注 PDF 页码，例如：

- 来源：PDF 第 92～95 页
- 对应章节：9.1 Caching of code and data

6. 当 PDF 中的观点可能受年代、CPU 型号或编译器版本影响时，必须注明适用范围，不要把它表述为永远正确的规则。

==================================================
三、Markdown 笔记规范
==================================================

每个 note/*.md 文件至少包含以下结构：

# 标题

## 1. 本章解决什么问题

使用通俗语言说明本章的目标。

## 2. 核心概念

解释本章涉及的术语。

## 3. 工作原理

说明 CPU、内存、编译器或操作系统内部发生了什么。

## 4. PDF 核心观点

按照 PDF 内容整理，不要逐句翻译。

每条关键观点注明 PDF 页码。

## 5. 简单示例

使用短小的 C++ 代码解释概念。

## 6. 未优化代码

展示常见的低性能写法。

## 7. 优化后代码

展示对应优化方式。

## 8. 为什么会更快

从以下角度分析：

- 指令数量；
- Cache 命中率；
- 内存访问次数；
- 分支预测；
- 指令依赖；
- 向量化；
- 函数调用；
- 动态内存分配；
- 编译器优化能力。

## 9. 如何验证

给出：

- 编译命令；
- 运行命令；
- Benchmark 方法；
- perf 命令；
- 查看汇编的方法。

## 10. 常见误区

解释容易误解的地方。

## 11. 实践任务

给出 3～5 个由简单到困难的练习。

## 12. 本章总结

用简洁语言总结。

## 13. 对应代码

列出 src 中对应的源码路径。

==================================================
四、代码示例规范
==================================================

所有代码必须放入 src，不要把完整项目代码只写在 Markdown 中。

每个实验目录建议包含：

example_name/
├── baseline.cpp
├── optimized.cpp
├── benchmark.cpp
├── README.md
└── CMakeLists.txt

代码必须满足：

1. 使用 C++17；
2. 在 Linux 下使用 GCC 或 Clang 编译；
3. 尽量不依赖大型第三方库；
4. 所有代码必须能够独立编译；
5. 不允许只写伪代码；
6. 不允许留下 TODO；
7. 不允许引用不存在的头文件；
8. 不允许使用未定义函数；
9. 对 SIMD 指令集进行编译期保护；
10. 对不支持 AVX2 或 AVX-512 的 CPU 给出清晰提示；
11. 不要为了展示优化而引入未定义行为；
12. 防止编译器将 Benchmark 代码完全优化掉；
13. 使用固定随机种子，使结果可复现；
14. Benchmark 执行多轮，输出平均值、中位数、最小值；
15. 运行前进行预热；
16. 明确区分初始化时间和计算时间。

Benchmark 中可以使用以下方法避免被优化掉：

- 返回计算结果；
- 将结果写入 volatile；
- 使用编译器屏障；
- 输出校验和；
- 比较 baseline 和 optimized 的计算结果。

==================================================
五、必须实现的实验
==================================================

至少实现以下实验。

1. Profiling

- 使用 std::chrono 测量函数耗时；
- 使用 perf stat；
- 使用 perf record；
- 使用 perf report；
- 展示热点函数定位。

2. 整数和浮点数

- int32_t 与 int64_t；
- float 与 double；
- 整数除法；
- 浮点除法；
- 类型转换的成本；
- 避免混合 float 和 double。

3. 分支预测

- 随机数据分支；
- 排序数据分支；
- branchless 写法；
- 比较 branch-misses。

4. 循环优化

- 循环展开；
- 循环不变量外提；
- 减少循环依赖；
- 多累加器；
- 循环融合与循环拆分。

5. 函数调用

- 普通函数；
- inline；
- static；
- 虚函数；
- 函数指针；
- std::function；
- Lambda。

6. 类和虚函数

- 普通成员函数；
- 虚函数；
- 继承；
- RTTI；
- dynamic_cast；
- 构造和析构成本。

7. 数据结构

- std::vector；
- std::list；
- std::deque；
- std::map；
- std::unordered_map；
- 连续内存与链式结构遍历性能比较。

8. 内存访问

- 顺序访问；
- 跨步访问；
- 随机访问；
- 不同数组规模；
- L1、L2、L3 Cache 效果；
- Cache miss 现象。

9. 数据布局

- AoS；
- SoA；
- 比较向量化能力和 Cache 利用率。

10. 内存对齐

- alignas；
- aligned_alloc；
- 普通内存与对齐内存；
- SIMD load/store。

11. 动态内存分配

- 循环内 new/delete；
- 预分配；
- reserve；
- 对象池或内存池的简单实现。

12. 多线程

- 单线程与多线程；
- std::thread；
- std::atomic；
- mutex；
- reduction；
- 线程数量扩展测试。

13. False Sharing

- 两个线程更新同一 Cache Line；
- 使用 alignas(64) 消除 False Sharing；
- 比较执行时间。

14. 自动向量化

- 可向量化循环；
- 不可向量化循环；
- restrict 或等效编译器提示；
- 查看 GCC/Clang 向量化报告。

15. SIMD Intrinsics

至少实现：

- SSE；
- AVX2；
- AVX-512 条件示例；
- 向量加法；
- 点积；
- reduction；
- min/max；
- RGB 或三维向量处理示例。

16. CPU Dispatch

实现：

- CPUID 检测；
- scalar 版本；
- SSE 版本；
- AVX2 版本；
- 运行时选择最佳实现。

17. Lookup Table

对比：

- 直接计算；
- 查表；
- 不同表大小；
- Cache 对性能的影响。

18. 性能测试陷阱

演示：

- Debug 与 Release；
- 第一次运行与预热后运行；
- 数据规模过小；
- 编译器消除代码；
- CPU 频率变化；
- 多进程干扰；
- 测试结果波动。

==================================================
六、编译系统
==================================================

在 src/CMakeLists.txt 中统一管理所有实验。

至少提供以下构建类型：

- Debug
- Release
- RelWithDebInfo

默认要求：

- C++17；
- 开启警告；
- Release 使用 -O3；
- 保留可选的 -march=native；
- 不要默认强制启用 AVX-512；
- AVX2 和 AVX-512 示例单独设置编译参数。

推荐编译参数：

-Wall
-Wextra
-Wpedantic
-O3
-g
-fno-omit-frame-pointer

对于自动向量化分析，增加可选参数：

GCC：
-fopt-info-vec
-fopt-info-vec-missed

Clang：
-Rpass=loop-vectorize
-Rpass-missed=loop-vectorize
-Rpass-analysis=loop-vectorize

README 中解释每个参数的作用。

==================================================
七、脚本要求
==================================================

scripts/build.sh：

- 创建 build；
- 配置 CMake；
- 编译所有示例；
- 遇到错误立即停止。

scripts/run_all.sh：

- 顺序运行所有示例；
- 清晰打印实验名称；
- 某个示例失败时记录错误。

scripts/benchmark_all.sh：

- 运行全部 Benchmark；
- 将结果保存到 benchmark_results/；
- 每次运行生成带时间戳的结果文件。

scripts/perf_profile.sh：

支持类似：

./scripts/perf_profile.sh ./build/08_memory_cache/cache_stride

执行：

perf stat
perf record
perf report

并提示可能需要修改 perf_event_paranoid。

==================================================
八、文档索引
==================================================

note/README.md 必须包含：

1. 项目介绍；
2. PDF 基本信息；
3. 建议学习顺序；
4. 每篇笔记的链接；
5. 每个实验的链接；
6. 编译方法；
7. 运行方法；
8. perf 使用方法；
9. 查看汇编的方法；
10. 推荐学习路线。

建立三条学习路线：

### 路线一：C++ 性能优化基础

适合只掌握基础 C++ 的读者。

### 路线二：Linux 性能分析

重点学习 perf、Benchmark 和热点分析。

### 路线三：CPU 与 SIMD 优化

重点学习 Cache、流水线、乱序执行、自动向量化和 Intrinsics。

==================================================
九、讲解深度
==================================================

不要只给出“某种写法更快”的结论。

必须解释：

- 为什么更快；
- 在什么 CPU 上可能更快；
- 编译器是否可能自动完成该优化；
- 优化是否依赖数据规模；
- 优化是否增加代码复杂度；
- 优化是否影响可移植性；
- 优化是否可能降低其他平台性能；
- 如何通过汇编、perf 或 Benchmark 验证。

特别强调：

“先测量，再优化。”

不要对非热点代码进行无意义的微优化。

==================================================
十、执行方式
==================================================

请分阶段执行，不要一次性草率生成所有内容。

阶段一：

1. 分析 PDF 目录；
2. 创建目录；
3. 编写 note/00_全书导读.md；
4. 编写 note/README.md；
5. 给出完整任务清单；
6. 暂时不要生成全部代码。

阶段二：

按 PDF 章节顺序生成 Markdown 笔记。

每完成一篇笔记：

1. 检查章节内容是否完整；
2. 检查页码引用；
3. 检查是否包含对应实验；
4. 更新 note/README.md。

阶段三：

实现 src 中的代码实验。

每完成一个实验：

1. 编译代码；
2. 修复所有编译错误；
3. 运行 baseline 和 optimized；
4. 验证输出结果一致；
5. 记录运行命令；
6. 更新对应 README；
7. 更新总 CMakeLists.txt。

阶段四：

完成整体检查：

1. 从空 build 目录重新编译；
2. 运行全部示例；
3. 检查所有 Markdown 链接；
4. 检查所有源码路径；
5. 检查是否有空文件；
6. 检查是否有 TODO；
7. 检查是否有不可编译代码；
8. 生成最终完成报告：

note/19_项目完成报告.md

==================================================
十一、质量要求
==================================================

禁止：

- 只翻译 PDF；
- 只写概念不写代码；
- 只写代码不解释原理；
- 编造实验结果；
- 编造性能提升百分比；
- 未实际编译就声称代码可以运行；
- 把 Debug 测试结果当作性能结论；
- 使用单次运行结果得出结论；
- 忽略 CPU、编译器和操作系统差异；
- 随意使用 AVX-512 导致程序在普通 CPU 上崩溃。

对于无法在当前机器验证的实验，必须明确写：

“当前环境未验证，以下为理论分析或待验证实现。”

现在开始执行阶段一。
先读取 optimizing_cpp.pdf 的目录和章节结构，然后创建项目规划。


继续执行阶段二。

本轮只处理 PDF 第 1～3 章。

要求：
1. 完整阅读对应页面；
2. 创建对应的 note Markdown；
3. 标注 PDF 页码；
4. 暂时不实现后续章节；
5. 完成后检查 Markdown 链接和内容完整性。


继续执行阶段三。

本轮只实现以下实验：

1. src/03_branch
2. src/08_memory_cache
3. src/11_false_sharing

要求：
1. 每个实验包含 baseline、optimized 和 benchmark；
2. 使用 C++17；
3. 接入项目总 CMakeLists.txt；
4. 实际执行 cmake 和编译；
5. 运行并验证 baseline 与 optimized 结果一致；
6. 不要编造性能数据；
7. 将运行方法和分析写入各目录 README.md。


现在对整个项目进行最终验收。

请完成：

1. 删除旧 build 目录；
2. 从头执行 CMake 配置和编译；
3. 运行所有示例；
4. 检查所有 Markdown 链接；
5. 搜索并处理 TODO、占位符和伪代码；
6. 检查每个 note 是否有 PDF 页码来源；
7. 检查 src 中代码是否被 note 正确引用；
8. 检查 SIMD 代码是否有 CPU 能力保护；
9. 检查 Benchmark 是否防止编译器消除；
10. 将验收结果写入 note/19_项目完成报告.md。

不得在没有实际验证的情况下声称测试通过。