# 19 项目完成报告（最终验收）

> 本报告依据任务书"对整个项目进行最终验收"的 10 项要求，逐项给出**实际执行命令与实测输出**。所有结论均来自真实运行，未做任何未经验证的声称。

## 1. 验证环境

| 项目 | 值 |
|------|-----|
| CPU | Intel Core i9-14900HX（P+E 混合架构），32 逻辑线程 |
| 指令集 | SSE4.2 / AVX / AVX2；**无 AVX-512** |
| 编译器 | g++ 13.3.0（Ubuntu 24.04）；无 clang |
| CMake | 4.1.3 |
| perf | 6.17.13，`perf_event_paranoid=4`（无免密 sudo） |

## 2. 验收清单（10 项，含实测证据）

### 1. 删除旧 build 目录

✅ 执行 `rm -rf build` 后 `ls build` 返回码 2（目录不存在），确认已删除。

### 2. 从头执行 CMake 配置和编译

✅ 执行：

```bash
cmake -S src -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j$(nproc)
```

实测输出：`errors: 0`，`warnings: 0`，`built targets: 58`（57 个可执行 + 1 个静态库 oopt_common）。18 个实验目录 + common 全部构建。

### 3. 运行所有示例

✅ 执行 `./scripts/run_all.sh`，退出码 **0**。

实测输出：`OK count: 57`，`FAILED lines: none`。57 个实验可执行全部运行成功。

### 4. 检查所有 Markdown 链接

✅ 脚本逐个解析：

- `note/README.md` 中 20 个笔记链接（00-19 + README）→ 全部存在；
- README 中 18 个 `../src/*` 实验链接 → 全部存在；
- 笔记中引用的 `src/*` 目录与源码文件 → 全部存在（见第 7 项）。

### 5. 搜索并处理 TODO、占位符和伪代码

✅ 实测：

```
grep -rniE "TODO|FIXME|TBD|XXX|PLACEHOLDER|PSEUDO" src/   -> 无匹配
grep -rniE "TODO|FIXME|TBD|PLACEHOLDER" note/*.md           -> 仅 19 报告自身描述验收项
find src note scripts -type f -size 0                       -> 无空文件
```

### 6. 检查每个 note 是否有 PDF 页码来源

✅ 逐篇统计：18 篇主题笔记全部含 `PDF 第 N 页` 引用，合计 **964 处**。

- 无页码引用缺失（00 导读为映射表 18 处、18 学习路线为综合章节 1 处，均含引用）；
- 越界检查：所有页码均在 PDF 1-179 合法范围内（`awk '$1>179'` → 无输出）；
- 高频引用页码（21/168/114/4/16/112/172/15/111/85）对应真实核心章节（内存与吞吐、预热、多累加器、引言、profiler、缓存行/多线程、嵌入式、时钟周期、多线程、编译选项）。

### 7. 检查 src 中代码是否被 note 正确引用

✅ 双向核对：

- A) note 引用的 18 个 `src/*` 路径 → 全部存在；
- B) 18 个实验目录 → 每个被 5~13 篇笔记引用（如 `18_benchmark` 13 篇、`01_profiling`/`04_loop` 8 篇），无"有代码无笔记引用"的目录。

### 8. 检查 SIMD 代码是否有 CPU 能力保护

✅ 代码层面 + 实际运行双重验证：

- 总 `src/CMakeLists.txt`：未全局开启 AVX-512（`grep avx512` → 无匹配）；
- `13_avx512_example`：**单独目标** `-mavx512f -mavx512vl -mavx512bw`，运行时 `cpu_has_avx512()` 保护。实测本机运行输出 `AVX-512 NOT supported...` 并退出码 0（安全，不崩溃）；
- AVX2 函数（`13_intrinsics/optimized.cpp`、`benchmark.cpp`）：`__attribute__((target("avx2")))` + 运行时 `cpu_has_avx2()` 选择，避免整 TU 编译为 AVX2；
- `14_cpu_dispatch`：AVX2 实现独立 TU（仅该文件 `-mavx2`），运行时 `cpu_instruction_set_level()` 选择，支持 `--force` 强制分支。实测 `--force avx2` 通过。

### 9. 检查 Benchmark 是否防止编译器消除

✅ 机制 + 实测：

- `src/common/benchmark.h`：被测返回值折叠进 `volatile long long sink`（防消除）；
- 使用 benchmark.h 的源文件 22 个；使用 `volatile` 的 24 个；含校验和/一致性检查输出的 30 个；
- `18_eliminate` 实测输出 `work kept: 4999999950000000`（防消除生效）；
- 优化差异真实性确认（非被消除后的假象）：
  - `13_intrinsics`：标量归约 3240 us vs AVX2 726 us（约 4.5 倍）；
  - `18_size`：0 MiB 2.27 us → 128 MiB 7005 us（缓存层级约 3000 倍台阶）；
  - `10_multithreading`：serial 30160 us vs x8 11100 us（约 2.7 倍加速）。

### 10. 将验收结果写入本报告

✅ 本文件即交付物；上述全部条目由实际命令输出支撑。

## 3. 验收结论

| 验收项 | 结果 |
|--------|------|
| 1 删除旧 build | ✅ |
| 2 从头配置编译 | ✅ 0 错误 0 警告，58 目标 |
| 3 运行所有示例 | ✅ 57 个全部通过，退出码 0 |
| 4 Markdown 链接 | ✅ 全部解析 |
| 5 TODO/占位符/伪代码 | ✅ 无；无空文件 |
| 6 note 页码来源 | ✅ 964 处，全部在 1-179 范围 |
| 7 src 代码被 note 引用 | ✅ 18 目录双向核对通过 |
| 8 SIMD CPU 保护 | ✅ 实测安全（AVX-512 跳过、AVX2 检测、dispatch） |
| 9 Benchmark 防消除 | ✅ 实测非零结果 + 优化差异真实 |
| 10 完成报告 | ✅ 本文件 |

**最终结论：项目验收通过。**

## 4. 环境限制说明（如实声明，非项目缺陷）

| 项目 | 说明 |
|------|------|
| AVX-512 实际执行 | 本机无 AVX-512；代码已带运行时保护并实测安全退出，掩码路径为"当前环境未验证，仅编译验证" |
| perf 硬件计数 | `perf_event_paranoid=4` 且无免密 sudo，无法实测 `branch-misses`/`cache-misses` 数值；`scripts/perf_profile.sh` 已实现权限提示与优雅降级 |
| Clang 向量化报告 | 未安装 clang；笔记记录等价选项（`-Rpass=loop-vectorize` 等） |
| 嵌入式实验 | 无交叉编译工具链，笔记标注为理论分析 |

## 5. 追加更新（工程深度扩展）

在验收通过后，按用户要求追加"工程深度及常见工作中的坑"：

- 新增 `note/20_工程实践与常见坑.md`：性能工程闭环（预算→基准→热点→优化→A/B→回归→监控）、可持续基准/CI 性能门禁方法论、按 8 类整理的实战坑库（编译器/测量/缓存/多线程/SIMD/浮点/链接/生产）。核心 13 节结构 + 第 14 节"对应代码"；41 处 PDF 页码引用、19 处"补充说明"标注（超出 PDF 的工程经验均已标注）、无 Unicode 连字符。
- 新增 `scripts/compare_versions.sh`：A/B 性能回归对比工具。实测验证：`debug vs release` 判定 REGRESSION（退出码 1，+260%）；`release vs debug` 判定 OK（退出码 0）；缺参数退出码 2。已通过 `bash -n` 语法检查。
- 索引同步：`note/README.md`（笔记链接 + 脚本用法 + 学习路线收尾）与 `note/00_全书导读.md`（映射表 + 任务清单）已更新。
