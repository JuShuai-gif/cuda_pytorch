#pragma once

namespace chp {

// Prints OS, CPU and compiler information to stdout.
// Used by benchmarks to record the environment the numbers were gathered in.
void print_system_info();

}  // namespace chp
