#pragma once

// ============================================================================
// 锁竞争下 NMS 中使用的检测框
// ============================================================================
struct DetectionBoxNMS {
    float x, y, w, h;
    float confidence;
    int class_id;
    bool suppressed;
};

// ============================================================================
// 演示: 在共享 DetectionBox 向量上使用自旋锁 vs 互斥锁 vs 无锁方式运行 NMS
// ============================================================================
void demo_lock_contention();
