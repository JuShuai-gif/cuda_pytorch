# FastVideo 学习规划

> 目标源码：`/home/ghr/code/FastVideo`
> 配套笔记：本目录 `index.md` 已建好完整笔记体系（00~06 七大类）。本文件是**时间盒执行计划**，把「读哪些文件 / 跑哪些命令 / 产出什么」按周排布，每步都绑定到具体源码与已有笔记。
>
> 用法：每完成一个 checkbox 就打勾；每周结束回答一次「本周自测题」，答不上就回炉。

---

## 0. 前置准备（0.5 天）

- [ ] 装环境（uv）：跑通 `python -c "import fastvideo, torch; print(torch.cuda.is_available())"` 和 `fastvideo --help`
  - 参考：`README.md` 第 49~87 行；笔记 `00_overview/01_install_and_env.md`
- [ ] 通读 `FastVideo/AGENTS.md` 与 `fastvideo/AGENTS.md`（作者给 AI/贡献者的地图，最快建立全局观）
- [ ] 通读本目录 `index.md` 一遍，知道遇到问题去哪个 `.md` 查

**自测**：FastVideo 一句话定位是什么？（后训练 + 实时推理的视频生成框架）

---

## 第 1 周：跑通 + 宏观推理链（对应笔记阶段 1）

**目标**：3 行代码生成视频，脑中能画出 prompt → mp4 的宏观流程。

### 要跑
- [ ] `python examples/inference/basic/basic.py`（Wan2.1 T2V 1.3B）
- [ ] 在 `pipelines/composed_pipeline_base.py` 的 `forward()` 里 `for stage in stages` 处，逐 stage 打印输出张量形状

### 要读（按顺序，抓主干不求全懂）
- [ ] `fastvideo/__init__.py` —— 4 个导出是什么
- [ ] `fastvideo/entrypoints/video_generator.py` —— `from_pretrained()` / `generate_video()`
- [ ] `fastvideo/api/sampling_param.py` —— `SamplingParam` 字段（分辨率/帧数/步数/guidance）
- [ ] `fastvideo/pipelines/composed_pipeline_base.py` —— stage 组合（全项目心脏）

### 配套笔记
- `03_core_flows/00_video_generation_flow.md`（最重要，完整推理链）
- `01_architecture/00_global_architecture.md`（Facade/Executor/Worker 三级分层）
- `04_knowledge_expansion/00_video_diffusion_basics.md`

**本周自测**：调用 `generate_video` 后，prompt 如何一步步变成 mp4？scheduler 和 VAE decode 分别在哪一步被调用？

---

## 第 2 周：模型与 Pipeline 内部（对应笔记阶段 2 上半）

**目标**：读懂 7 个 stage 的数据流，看懂一个 DiT（Wan）的 forward。

### 要读：stages
- [ ] `pipelines/stages/text_encoding.py`（prompt → embedding）
- [ ] `pipelines/stages/latent_preparation.py`（初始化噪声 latent）
- [ ] `pipelines/stages/timestep_preparation.py`（`scheduler.set_timesteps`）
- [ ] `pipelines/stages/denoising.py`（**最核心**：去噪循环 + CFG + `scheduler.step`）
- [ ] `pipelines/stages/decoding.py`（VAE decode）
- [ ] `pipelines/pipeline_batch_info.py` —— `ForwardBatch`（贯穿所有 stage 的数据载体）

### 要读：DiT
- [ ] `fastvideo/models/dits/wanvideo.py` —— `WanTransformer3DModel.forward`：patch embed → blocks → unpatchify

### 配套笔记
- `02_source_by_directory/03_pipelines.md`、`04a_dit_wanvideo.md`
- `03_core_flows/04_prompt_to_video_tensor_flow.md`（全链路张量形状）
- `04_knowledge_expansion/01_dit_transformer_for_video.md`

**本周自测**：手画 denoising loop 内部（DiT forward + CFG + scheduler.step），标出每步张量形状。

---

## 第 3 周：Scheduler / VAE / Attention 后端（对应笔记阶段 2 下半）

**目标**：理解采样求解器、VAE 压缩、attention 后端选择机制。

### 要读
- [ ] `models/schedulers/scheduling_flow_match_euler_discrete.py` —— `scale_noise` / `step`
- [ ] `models/vaes/wanvae.py` —— encode/decode，理解 `[B,3,T,H,W]→[B,16,T/4,H/8,W/8]`
- [ ] `fastvideo/attention/layer.py` + `attention/selector.py`（后端选择）
- [ ] 环境变量 `FASTVIDEO_ATTENTION_BACKEND` 切换不同后端各跑一次，profiler 对比耗时

### 配套笔记
- `04_knowledge_expansion/03_vae_for_video.md`、`04_scheduler_sampling_solver.md`
- `04_knowledge_expansion/05_attention_acceleration.md`、`06_sparse_attention.md`
- `03_core_flows/05_scheduler_and_sampling_flow.md`、`06_vae_decode_flow.md`、`07_attention_backend_flow.md`

**本周自测**：flow matching 与 DDPM/DDIM 差别？为什么 VAE 时间维压 4 倍、空间维压 8 倍？

---

## 第 4 周：训练与微调（对应笔记阶段 3 训练部分）

**目标**：读懂训练主循环，能跑一次小数据 LoRA 微调。

### 要读
- [ ] `fastvideo/train/entrypoint/train.py` —— `run_training_from_config`
- [ ] `fastvideo/train/trainer.py` —— `Trainer.run`（梯度累积 + optimizer step + checkpoint）
- [ ] `fastvideo/train/methods/fine_tuning/finetune.py` —— MSE flow matching loss
- [ ] `fastvideo/train/utils/lora.py` —— `enable_lora_training`

### 要跑
- [ ] 用 `examples/train/configs/fine_tuning/wan/` 下配置跑一次小数据 LoRA 微调

### 配套笔记
- `02_source_by_directory/08_training.md`
- `03_core_flows/08_lora_finetune_flow.md`
- `04_knowledge_expansion/09_lora_finetuning.md`
- `06_practical_guides/05_how_to_train_lora.md`

**本周自测**：训练主循环里 loss 怎么算？LoRA 是怎样注入到哪些层的？

---

## 第 5 周：分布式（SP / FSDP / TP）

**目标**：看懂多卡推理/训练的并行策略与通信。

### 要读
- [ ] `fastvideo/worker/multiproc_executor.py`（spawn worker）
- [ ] `fastvideo/distributed/parallel_state.py` —— `GroupCoordinator`、`initialize_model_parallel`
- [ ] `fastvideo/distributed/device_communicators/base_device_communicator.py` —— `AllToAll4D`（SP 核心）
- [ ] `fastvideo/models/loader/fsdp_load.py` —— `maybe_load_fsdp_model` / `shard_model`
- [ ] `fastvideo/attention/layer.py` —— `DistributedAttention.forward`（SP 在 attention 的体现）

### 配套笔记
- `02_source_by_directory/07_distributed.md`
- `01_architecture/03_distributed_architecture.md`
- `03_core_flows/01_from_pretrained_flow.md`、`03_model_loading_flow.md`
- `04_knowledge_expansion/07_sequence_parallelism.md`、`08_fsdp_and_distributed_training.md`

**本周自测**：SP 下一条序列如何切分？all-to-all 在 attention 前后各做了什么？

---

## 第 6 周：CUDA Kernel（本仓库的重头，结合 cuda_pytorch 主题）

**目标**：走通 Python → CUDA 的调用链，能读一个 kernel 并用测试验证理解。

### 要读（自顶向下）
- [ ] `fastvideo/attention/backends/video_sparse_attn.py`（VSA Python 侧）
- [ ] `fastvideo-kernel/python/fastvideo_kernel/ops.py`（Python 封装）
- [ ] `fastvideo-kernel/csrc/common_extension.cpp`（pybind11 注册）
- [ ] `fastvideo-kernel/csrc/attention/block_sparse_h100.cu`（CUDA kernel）
- [ ] `fastvideo-kernel/csrc/turbodiffusion/norm/rmsnorm.cu` + 跑 `tests/test_turbodiffusion.py` 验证

### 配套笔记
- `01_architecture/04_kernel_architecture.md`
- `02_source_by_directory/11_fastvideo_kernel.md`
- `04_knowledge_expansion/11_cuda_kernel_and_pytorch_extension.md`、`12_flashattention_sageattention_flashinfer.md`
- `06_practical_guides/07_how_to_read_cuda_kernel.md`

**本周自测**：从 `torch` 层一个 attention 调用，到最终 `.cu` kernel，中间经过哪几层？pybind 如何把 C++ 暴露给 Python？

---

## 第 7 周（可选进阶）：蒸馏 / 稀疏注意力 / 实时应用

按兴趣挑，不必全做：

- [ ] **蒸馏**：`train/methods/distribution_matching/dmd2.py`（student/teacher/critic 三角色）
  - 笔记 `03_core_flows/09_distillation_flow.md`、`04_knowledge_expansion/10_distillation_dmd_sparse_distill.md`
- [ ] **稀疏注意力 VSA/STA**：论文 arXiv 2505.13389 / 2502.04507 + `04_knowledge_expansion/06_sparse_attention.md`
- [ ] **实时应用 Dreamverse**：`apps/dreamverse/` + 笔记 `02_source_by_directory/12_apps_dreamverse.md`
- [ ] **性能分析**：`profiler.py` + `06_practical_guides/06_how_to_profile_performance.md`（nsys / ncu）

---

## 三条源码主线（贯穿始终，随时对照）

1. **推理线**：`__init__` → `video_generator` → `composed_pipeline_base` → `stages/denoising` → `dits/wanvideo` → `vaes/wanvae`
2. **分布式线**：`worker/multiproc_executor` → `distributed/parallel_state` → `AllToAll4D` → `loader/fsdp_load` → `layers/linear`
3. **训练/kernel 线**：`train/trainer` → `train/methods` → `attention/backends/video_sparse_attn` → `fastvideo-kernel/csrc`

---

## 毕业标准（能改代码才算懂）

- [ ] 给已有模型加一个自定义 stage 变体（如自定义去噪步）——`06_practical_guides/02_how_to_add_new_pipeline.md`
- [ ] 加一个 attention 后端骨架——`06_practical_guides/03_how_to_add_attention_backend.md`
- [ ] 读懂并能修改一个 `.cu` kernel，测试通过

---

## 进度追踪

| 周 | 主题 | 状态 | 自测通过 |
|----|------|------|----------|
| 0 | 环境 + 全局观 | ☐ | ☐ |
| 1 | 跑通 + 推理链 | ☐ | ☐ |
| 2 | 模型 + pipeline | ☐ | ☐ |
| 3 | scheduler/VAE/attention | ☐ | ☐ |
| 4 | 训练 + LoRA | ☐ | ☐ |
| 5 | 分布式 | ☐ | ☐ |
| 6 | CUDA kernel | ☐ | ☐ |
| 7 | 进阶（可选） | ☐ | ☐ |
