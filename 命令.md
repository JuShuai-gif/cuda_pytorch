下面是一份 **CUDA 工程开发中最常用的 15 条 `nvcc` 命令 Cheat Sheet**。这些命令覆盖了 **编译、调试、性能分析、架构适配、库构建、底层指令分析** 等开发场景。

`nvcc` 是 NVIDIA CUDA Compiler 提供的 CUDA 编译驱动，用于把 `.cu` 文件编译为 **GPU 可执行程序或设备代码**。

---

# CUDA开发者常用 nvcc 命令（15条）

```text
1️⃣ nvcc kernel.cu -O3 -arch=sm_86 -o app
生成针对 sm_86 GPU 优化的可执行文件（最常用）

2️⃣ nvcc kernel.cu -o app
默认编译，生成 a.out 或指定名称的可执行文件

3️⃣ nvcc -c kernel.cu
生成 kernel.o 目标文件（用于大型工程链接）

4️⃣ nvcc kernel.cu util.cpp -o app
CUDA + C++ 混合编译

5️⃣ nvcc kernel.cu -ptx
生成 kernel.ptx（GPU中间代码）

6️⃣ nvcc kernel.cu -cubin
生成 kernel.cubin（GPU机器码）

7️⃣ nvcc kernel.cu -fatbin
生成 kernel.fatbin（多GPU架构二进制）

8️⃣ nvcc kernel.cu -g -G
生成可调试GPU代码（cuda-gdb）

9️⃣ nvcc kernel.cu -Xptxas -v
打印寄存器 / shared memory 使用

10️⃣ nvcc kernel.cu -keep
保留所有编译中间文件
（ptx、cubin、host stub等）

11️⃣ nvcc kernel.cu -lineinfo
生成带行号信息的优化代码（用于 profiler）

12️⃣ nvcc kernel.cu -maxrregcount=64
限制每个线程使用的寄存器数量

13️⃣ nvcc -shared -Xcompiler -fPIC kernel.cu -o libkernel.so
生成 CUDA 动态库

14️⃣ nvcc -lib kernel.cu -o libkernel.a
生成 CUDA 静态库

15️⃣ nvcc -v kernel.cu
显示完整 CUDA 编译流程
```

---

# CUDA开发者常用调试组合

## 1 查看 GPU 指令（SASS）

```bash
nvcc kernel.cu -cubin
cuobjdump -sass kernel.cubin
```

输出：

```
LDG.E
LDMATRIX
HMMA
```

这是 GPU 真正执行的机器指令。

---

## 2 查看 PTX 中间代码

```bash
nvcc kernel.cu -ptx
```

生成：

```
kernel.ptx
```

可以看到：

```
ldmatrix.sync
mma.sync
```

---

## 3 查看寄存器占用

```bash
nvcc kernel.cu -Xptxas -v
```

输出示例：

```
ptxas info    : Used 64 registers
ptxas info    : Used 48 bytes smem
```

用于判断：

* register pressure
* occupancy

---

# CUDA 编译流程（理解 nvcc 很重要）

CUDA 编译不是一步完成，而是：

```
.cu
 ↓
CUDA Frontend
 ↓
PTX
 ↓
ptxas
 ↓
SASS
 ↓
cubin
 ↓
link
 ↓
ELF 可执行文件
```

所以：

* `-ptx` → 中间表示
* `-cubin` → GPU机器码
* `-fatbin` → 多架构二进制

---

# 实际 CUDA 工程最常用 3 条命令

99% CUDA开发：

```bash
nvcc kernel.cu -O3 -arch=sm_86 -o app
```

性能分析：

```bash
nvcc kernel.cu -Xptxas -v
```

研究 GPU 指令：

```bash
nvcc kernel.cu -cubin
cuobjdump -sass kernel.cubin
```

---

💡 **结合你的研究方向（TensorCore / `ldmatrix` 指令）**，工程师通常用这一套 workflow：

```bash
nvcc mma.cu -arch=sm_80 -O3 -cubin
cuobjdump -sass mma.cubin
```

查看：

```
LDMATRIX
HMMA
```

Tensor Core 指令。

---

如果你愿意，我可以再给你一份 **CUDA kernel 开发者的完整调试工具链（nvcc + cuobjdump + nvdisasm + nsight）**，基本是 NVIDIA 内部调优 kernel 的 workflow，对研究 **TensorCore / warp-level instruction** 非常有帮助。
